@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: "Poppins", sans-serif;
}

body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;

    /* Soft user-friendly gradient */
    background: linear-gradient(135deg, #6C63FF, #A683FF, #C9A7FF);
    background-size: 300% 300%;
    animation: bgshift 12s ease infinite;
}

@keyframes bgshift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

.container {
    width: 360px;
}

.box {
    background: rgba(255, 255, 255, 0.2);
    padding: 40px;
    border-radius: 18px;
    backdrop-filter: blur(12px);
    box-shadow: 0 15px 50px rgba(0, 0, 0, 0.2);
    animation: slideUp 1s ease forwards;
    transform: translateY(40px);
    opacity: 0;
}

@keyframes slideUp {
    100% {
        transform: translateY(0);
        opacity: 1;
    }
}

h2 {
    text-align: center;
    color: #fff;
    margin-bottom: 25px;
    font-weight: 500;
}

.inputBox {
    position: relative;
    width: 100%;
    margin-bottom: 25px;
}

.inputBox input {
    width: 100%;
    padding: 10px;
    background: transparent;
    border: none;
    border-bottom: 2px solid #f0f0f0;
    color: #0000;
    outline: none;
    font-size: 16px;
    transition: 0.3s;
}

.inputBox span {
    position: absolute;
    left: 0;
    padding: 10px 0;
    color: #e0e0e0;
    pointer-events: none;
    transition: 0.35s;
}

.inputBox input:focus ~ span,
.inputBox input:valid ~ span {
    transform: translateY(-18px);
    font-size: 12px;
    color: #ffe6ff;
}

.inputBox input:focus {
    border-bottom-color: #ffe6ff;
}

.links {
    display: flex;
    justify-content: space-between;
    margin: -10px 0 20px;
}

.links a {
    color: #fff;
    font-size: 14px;
    text-decoration: none;
    transition: 0.3s;
}

.links a:hover {
    color: #ffe6ff;
}

.btn {
    width: 100%;
    padding: 12px;
    background: #ffffff;
    border: none;
    border-radius: 25px;
    color: #6C63FF;
    font-size: 16px;
    cursor: pointer;
    transition: 0.3s;
    font-weight: 600;
}

.btn:hover {
    background: #eaeaff;
    transform: scale(1.05);
}
